namespace = steeltypeaievent 
	country_event ={
		id = steeltypeaievent.1
		hide_window = yes
		is_triggered_only = yes
		fire_only_once = yes
		immediate = {
			every_country = {
				limit = {
					has_valid_civic = civic_EOE_steel_type_trainer
					is_ai = yes
				}
			}
			every_owned_planet = {
            limit = {
               is_colony = yes
              }
             add_modifier = {
					job_steel_type_trainer_add = 3
				}
          }
		}
	}
}